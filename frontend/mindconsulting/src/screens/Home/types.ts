export type TypeCourse = {
  id: string;
  name: string;
  category_id: string;
  teacher: string;
  description: string;
  image: string;
  active: boolean;
  createdAt: string;
  updateAt: string;
};
